[project]
name = "uvdemo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["requests>=2", "click"]

[project.optional-dependencies]
color = ["colorama"]

[dependency-groups]
dev = ["pytest"]
